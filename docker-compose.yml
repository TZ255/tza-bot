services:
  imma-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: imma-bot
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      - whatsapp_sessions:/app/.wwebjs_auth
    ports:
      - "4100:4100"
    networks:
      - bot-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:4100/", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  whatsapp_sessions:

networks:
  bot-network:
    driver: bridge